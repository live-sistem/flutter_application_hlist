import 'package:flutter/material.dart';

class NoteSettingsPopov extends StatelessWidget {
  const NoteSettingsPopov({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
